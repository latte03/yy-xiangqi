# 前端构建/运行环境变量（复制为 .env.local 后按需修改）

# 识别后端地址（本地 sidecar）。默认 http://127.0.0.1:8765
VITE_RECOGNIZE_API=http://127.0.0.1:8765

# 是否在构建中包含「模型训练」入口。
# - vite dev（开发）默认开启，无需设置。
# - 分发给终端用户的构建：保持 false / 不设置 —— 训练入口不编入包，训练是维护者离线活。
# - 维护者想在打包构建里保留训练台：设为 true。
VITE_ENABLE_TRAINING=false
