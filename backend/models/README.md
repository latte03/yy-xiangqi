# Bundled models

These files are the baseline ONNX models bundled into desktop releases.

Tracked:

- `piece_classifier.onnx`
- `board_locator.onnx`
- `labels.txt`
- `version.json`

Do not commit training checkpoints (`*.pt`), datasets, crops, or generated release
manifests. `models.json` is generated by the model release workflow.
